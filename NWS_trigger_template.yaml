# Append this to configuration.yaml

template:
  - trigger:
      - platform: time_pattern
        minutes: "/5"
      - platform: homeassistant
        event: start
    action:
      - action: weather.get_forecasts
        data:
          type: twice_daily
        target:
          entity_id: weather.kxxx_daynight
        response_variable: daily
      - action: nws.get_forecasts_extra
        data:
          type: twice_daily
        target:
          entity_id: weather.kxxx
        response_variable: weather_forecast
    sensor:
      - name: kxxx daynight
        state: "{{ states('weather.kxxx_daynight') }}"
        icon: mdi:weather-cloudy
        attributes:
          forecast: "{{ daily['weather.kxxx_daynight'].forecast }}"
      - name: kxxx daynight extra
        state:  "{{ states('weather.kxxx_daynight') }}"
        icon: mdi:weather-cloudy
        attributes:
          forecast: "{{ weather_forecast['weather.kxxx_daynight'].forecast }}"
